[access]
	inheritFrom = All-Users
[project]
	description = Works with the LiveGrapherHost module in our robot code to graph data from the robot (useful for tuning PID loops).
[submit]
	mergeContent = false
	action = rebase if necessary
